#include <stdio.h>
#include <stdlib.h>
/*program that prints number of arguments passed on to it
 *returns 0 successfully
 */
int main(int argc, char *argv[])
{
  printf("argc = %d\n", argc);
  return (0);
}
